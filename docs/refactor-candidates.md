# リファクタ候補一覧

このファイルは、今後の改善候補を整理するためのメモです。

## 優先度高

### 1. `GameScene` のオーケストレータ化を完了する

進めたいこと:

- `game_scene.cpp` は更新順制御中心にする
- ドメイン固有ロジックは各ドメイン `.cpp` へ移譲する

### 2. Enemy/Gimmick 共通ダメージ判定の共有化

進めたいこと:

- 接触ダメージ処理を共有ヘルパへ寄せる
- 仕様変更時の修正箇所を 1 箇所にする

### 3. `components.*` の責務整理

進めたいこと:

- 写真系、敵系、ギミック系の境界コメントを強化
- 将来的な分割ポイントを明確化

## 優先度中

### 4. `photo_system.cpp` を完全ファサード化

進めたいこと:

- bridge 関数だけを残し、ロジックは `capture/paste/shared` 側へ集約

### 5. `game_scene_gameplay.cpp` の更なる軽量化

進めたいこと:

- Player / Enemy / Gimmick 呼び出し順だけ残す
- ユーティリティ処理をドメイン側へ移す

### 6. ドキュメント更新フロー固定

進めたいこと:

- PR テンプレートに docs 更新チェックを追加
- `docs/domain-ownership-guide.md` を基準文書にする

## 優先度低

### 7. ビルド構成の将来改善

進めたいこと:

- `.vcxproj` 競合を減らす運用整理
- 将来的なビルド構成自動化の検討
